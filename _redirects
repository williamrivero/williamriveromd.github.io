# Rewrite extensionless guide URLs → serve .html content directly (no HTTP redirect)
# Fixes redirect loop: Pretty URLs strips .html, old 301 rules added it back
/guides/:slug  /guides/:slug.html  200
